{
  "name": "indic-voice-workers",
  "version": "6.0.0",
  "private": true,
  "description": "Cloudflare Workers API gateway for Indic Voice AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "type-check": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.40.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
