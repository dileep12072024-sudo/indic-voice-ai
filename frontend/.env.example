# Copy this file to .env.local and fill in values
# During development Vite proxies /api → http://localhost:8000
# so VITE_API_BASE is only needed for standalone / production builds.
VITE_API_BASE=http://localhost:8000
